{
    "create": "CREATE TABLE IF NOT EXISTS courses (id INT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(50) NOT NULL UNIQUE);",
    "drop": "DROP TABLE IF EXISTS courses;",
    "insert": "INSERT INTO courses (name) VALUES (%s);",
    "fetch": {
      "all": "SELECT id, name FROM courses;"
    }
 }